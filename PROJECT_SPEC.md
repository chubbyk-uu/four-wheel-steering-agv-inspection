# 四轮独立驱动与独立转向 AGV 巡检平台项目规范

更新日期：2026-09-08

阅读入口：[当前状态](docs/CURRENT_STATUS.md)、[文档索引](docs/README.md)。本规范保存完整目标和约束，归档阶段报告不作为现行参数来源。
状态：550 kg四悬挂模型、八电机基本控制、双C8点云与安装几何已接入。OptiX共享几何/颜色＋法线、有界缓存与原图归档已实现；旧模型完成20 m全宽及100 m中央走廊专项测试，新模型低速GUI成像已复测，平场/独立11 kHz与高速长时采集仍待复测；新版10 km/h稳速20 m短测已通过。矩形规划/预览和三维融合定位已实现；矩形时间轨迹闭环与可选采集联动已接入，普通暂停恢复（保留未满帧）已接入，覆盖审计与独立补扫请求已接入；跨会话覆盖汇总、避障及连续条带拼接尚待实现。当前安装和验证见[重建记录](docs/LARGE_AGV_REBUILD.md)，历史阶段记录保留原始参数。

本文记录本项目已确认的目标、设计约束和实施顺序。标为“暂定”或“待验证”的内容不是已确认的机械规格或性能承诺；后续变更应同步更新本文。

2026-09-08新版生效：550 kg、车壳1.9×1.1 m、顶盖1.1 m、四轮直径0.4 m及悬挂，GNSS间距1.1 m/顶端1.2 m，前两侧C8；20 mm/4K/1.5 m、0.3662 mm等距行触发、10 km/h约7.59 kHz，扫描轨迹间距1 m。旧版65 kg/16 mm结果为历史验证。当前参数与验证以[重建记录](docs/LARGE_AGV_REBUILD.md)为准。

## 1. 项目目标

基于 ROS 2 与 Gazebo Sim（下文简称 GZ），建立室外四轮独立驱动、独立转向移动机器人平台，实现：

- 参数化底盘建模、八电机基本控制及有限转角下的运动分配。
- 双天线RTK GNSS位置/航向、IMU与四轮里程计的三维融合定位。
- 巡检区域覆盖路径规划、避障、采集控制与漏扫区域管理。
- 编码器触发的线阵成像、连续图块输出与位置标签归档。
- 线阵光学畸变校正、运动几何校正、平场校正和地面图像拼接。
- 使用带真实地面纹理和几何缺陷的场景，对整条链路进行可重复验证。

当前先做水平地面巡检，允许局部小凹坑、裂缝和表面破损，但不得影响轮组正常接地与滚动，不以轮胎跨坑、离地或车身颠簸作为本阶段工况。缺陷尺寸、深度和位置应据轮组通过情况验证，不能仅凭“小”就默认无影响。用户于2026-09-08补充最终支持高度变化路面：从当前阶段就采用三维地表规划、完整姿态估计和接触约束控制。当前验收场景仍为平面，后续分阶段加入坡面与可通过的高差，不默认现有刚性轮组可通过任意起伏。

## 2. 已确认参数与暂定参数

| 项目 | 基线 | 状态 |
| --- | --- | --- |
| 轮组 | 四个，每轮一个驱动电机、一个转向电机，共八个电机 | 已确认 |
| 整车质量 | 550 kg，含电池 | 用户修正 |
| 最高车速 | 真车约 20 km/h；当前仿真上限 10 km/h（2.778 m/s） | 仿真控制器和驱动关节限速同步；转向与扫描仍受工况约束 |
| 转向机构 | 无电滑环，正反方向均有限位，总转动空间约 380° | 已确认约束；准确端点待机械方案确定 |
| 相机横向像素 | 按 4096 像素（4K）设计 | 当前基线 |
| 标称地面扫描宽度 | 1.5 m | 当前基线 |
| 标称横向采样间距 | 1500 / 4096 = 0.3662109375 mm/像素 | 已确认目标 |
| 标称纵向采样间距 | 与横向相同 | 已确认目标 |
| 镜头选型方向 | 20 mm 焦距，像场覆盖完整感光线并留余量 | 用户认可的优先方向；具体镜头待选 |
| 最终场景规模 | 代表性跑道 100 m × 10 m，支持更宽跑道 | 长宽可配置，10 m 不是宽度上限 |
| 持续有效行频 | 11 kHz | 按墙钟吞吐验收；旧模型专项通过，新模型整链路待复测 |
| 图块高度 | 暂定连续 4096 行，可配置 | 对应约1.5 m沿程分块长度；2048行对应约0.75 m |
| 标称单图块覆盖 | 宽1.5 m、沿程长约1.5 m | 理想平面与有效等距离采样条件下 |
| 补光 | 仅实现一种，默认采用高亮度条形 LED | 用户允许 LED 或线激光，无需两种模式；后续如改选则替换方案 |
| 软件环境 | ROS 2 Jazzy + Gazebo Harmonic | 当前已使用 |
| 当前仿真质量 | 550 kg | 用户确认，可配置 |
| 初始车体尺寸 | 车壳1.9×1.1 m，含两雷达1.3 m；轮径0.4 m；估算轴距1.3 m、轮距0.94 m | 暂定，不能作为实车机械规格 |
| 初始转向硬限位 | −190°～+190°，另配置内侧软限位 | 暂定，实际零位与端点待确认 |

0.3662 mm/像素表示地面采样间距，不等同于可可靠识别同等宽度缺陷。实际分辨能力还受镜头、曝光、对焦、噪声和运动影响。

## 3. 底盘模型与控制

### 3.1 机械与动力学

- 使用参数化模型描述车架、电池、轮组及传感器的尺寸、质量、质心和惯量。
- 四个转向关节使用有限转角关节；四个驱动关节允许持续旋转。
- 建立轮地接触、摩擦及执行器速度、加速度和力矩约束。
- 最高速度、扫描速度、横移速度、角速度和加减速分别配置。
- 后续起伏地面验证需考虑轮胎顺应性或悬挂，不能默认刚性四轮始终均匀接地。
- 相机支架安装外参可配置；若研究镜头或支架相对车体的抖动，应单独建立相机相对运动模型。

### 3.1.1 外形与配色参考

外观参考用户提供的 `53563092.png`（2026-09-05 会话图片），大致相似即可，不要求精确复刻。

- 采用低矮、宽体的箱式车身，顶部与前部有适度斜面和分块盖板。
- 主体使用橙色，搭配白色或浅灰色顶盖及侧面横条，轮胎使用黑色，轮组支架使用灰色金属外观。
- 四个轮组布置在车体下方，保留外露转向和驱动结构的视觉特征；可加入简化顶部警示灯。
- 参考图仅约束外形风格，不作为八电机内部结构、准确尺寸、悬挂或动力学参数的依据。
- 模型优先使用简单几何和参数化结构；外观细节不得妨碍轮组转向空间、碰撞与传感器视线。

参考图片目前仅作为会话附件，尚未复制入仓库；不依赖用户桌面绝对路径加载模型。

### 3.2 运动能力与转向分配

实现直行、倒车、横向平移、斜行、原地旋转及平移与旋转组合运动。

对车体速度 `(vx, vy, wz)`，位于车体坐标 `(xi, yi)` 的轮组，其理想平面速度为：

```text
wheel_vx = vx - wz * yi
wheel_vy = vy + wz * xi
```

由此求轮向与驱动速度，并利用 `(theta, speed)` 与 `(theta + pi, -speed)` 的运动等价性选择可行解。

控制器必须：

- 保存未按整圈取模的实际机械转角，不能将有限转角机构当成无限旋转关节。
- 在硬限位以内枚举等价轮向，考虑软限位余量、转向距离、转向速率和驱动反向切换。
- 直行机械零位必须位于转向行程内部，不能把机械端点或角度取模断点放在常用的直行小角度调整区。当前约定朝前为 0°、硬限位为 ±190°。静止重新分配时，先排除限位余量不足的解，再优先选各轮实际机械转角最小的等价解；不采用停车后无条件回到 0°的规则。
- 正向、斜向和横向的原路退回均可保持原有轮向并反转驱动，不局限于直行。平移叠加旋转时应逐轮求解；只有整个车体速度向量一起取反，才必然对应所有轮速一起取反。
- 限位风险既可能由连续改变相对车体的轮向积累，也可能由多次离散最短转向选择积累。原地旋转和固定曲率运动不必积累舵轮转角；车体航向与轮组机械转角不能混为一谈。
- 在接近软限位前，考虑转向速率及停止转向所需行程，提前制动并重配置。只有明确触发限位重配置时才主动转到另一内部等价分支。
- 避免零速轮向跳变及等价解之间频繁抖动。
- 在大角度重配置时协调减速、必要停车和轮向调整，不允许瞬间切换运动模式。
- 根据实际转角与实际驱动速度计算里程计，不用指令速度代替测量，也不套用差速底盘模型。

## 4. 定位与地表坐标

- 定位组合为差分 GNSS / RTK、IMU 与四轮编码器里程计。
- 保存 `(x, y, z)` 与完整三维姿态，姿态接口优先采用四元数。
- 局部里程计保持连续，全局 GNSS 校正通过清晰的坐标关系接入；同一段 TF 只能有一个发布者。
- 保留合法横向速度，不使用差速机器人的横向零速度约束。
- 建模 GNSS 噪声、延迟、失锁和异常，IMU 偏置，以及编码器量化与轮胎打滑。
- 明确初始航向来源；单天线 GNSS 静止位置不能替代可靠的车头朝向。
- 校准 GNSS 天线、IMU、车体与相机外参，并统一时间基准。

地表处理分三个层次：

1. 水平面：二维覆盖规划，三维位姿记录。
2. 倾斜平面：在坡面局部 `(u, v)` 坐标中规划与拼接，再映射到三维世界；距离和面积按坡面实际尺度计算。
3. 起伏地表：暂不考虑，不列入当前或下一步任务。历史起伏场景仅保留为独立光学回归夹具。

三维定位不自动提供相机下方地形。起伏地表校正需要额外的地表模型或测距信息。RTK 全局定位精度也不能直接作为逐像素拼接精度。

## 5. 线阵采集约定

### 5.1 连续扫描与图块边界

相机沿前进方向连续扫描。横向为感光线方向，纵向由运动和行采样形成。横向固定4096像素，纵向每图行数可配置，默认4096行，即完整图块4096×4096。`linescan.yaml`的`block_rows`设置默认行数，C++/OptiX采集可用`scan_block_rows`启动参数覆盖（1～16384）；末尾短块保留实际行数。

```text
第 1～4096 行        → 图块 A
第 4097～8192 行     → 图块 B
第 8193～12288 行    → 图块 C
```

- 同一连续扫描轨迹内，图块是数据分包，前后连续，没有设计上的重叠。
- 不为拼接而重复使用上一图块的末尾扫描行。
- 不同扫描轨迹之间保留侧向重叠，重叠率待确定。
- 同轨迹内按行序与运动映射连续铺放；跨轨迹利用重叠区配准。
- 漏触发、丢行、倒退或恢复扫描可能产生真实缺口或重复覆盖，应显式记录，不能假装连续。
- 停止时不足配置行数的尾块应保留实际行数与终止原因，不填充成完整采集块。

### 5.2 编码器触发

- 标称每前进0.3662109375 mm 采集一行。
- 触发依据编码器计数及明确的轮组几何换算规则，不用 GNSS 更新频率驱动行采样。
- 明确编码器分辨率、减速比、轮径、触发倍频或分频关系；实际可实现间距待硬件确认。
- 驱动轮转动距离不总等于相机扫描距离，必须考虑转向、旋转中心偏置与打滑。
- 首版建议仅在固定车身朝向的直线扫描段采集，转弯、换行、避障和轮向重配置期间关闭采集。
- 反向扫描需要记录扫描方向，不能仅对编码器计数取绝对值而丢失方向语义。
- 固定线阵相机需要具有垂直于感光线地面投影的运动分量，不能认为底盘任意平移方向均可有效扫描。

### 5.3 位置标签与时间

用户要求图块参考位置对应最后采集的那一行，不采用面阵图像中心时刻的语义。

不要求逐行归档完整位姿。推荐基线为“图块标签 + 独立的固定频率融合位姿轨迹”，也允许每块保存一个或少量位置标签。

每块至少归档：

- 任务、轨迹、扫描段和图块编号。
- 宽度、实际行数、数据格式及全局行号范围。
- 首行、末行曝光参考时间及末行相机参考位姿。
- 首末编码器计数、标称行间距与扫描方向。
- 曝光、增益、光学与平场标定版本。
- 丢行、触发异常、暂停或中断等有效性信息。

曝光参考时间暂定采用曝光中点；真机接入时应按硬件时间戳定义换算，不能使用网络接收时刻代替。仿真统一使用仿真时间。

定位轨迹频率按运动变化与允许几何误差选择，不随行频等比例增长。处理时可从稀疏轨迹插值各行状态，但不能宣称恢复未测得的高频振动。非均匀触发时，可按需增加紧凑的行时间偏移或触发事件记录，而非逐行复制完整位姿。

### 5.4 速度与计算量

当前仿真上限 **10 km/h**，对应 **7585.19 行/s**，每个 4096 行图块约 **0.540 s**，Mono8 约 **31.07 MB/s**。11 kHz 成像能力可覆盖当前车速并留余量。以下 20 km/h 数值仅保留为真车上限的设计参考，不作为当前整车演示速度。

在 20 km/h、4096 像素/行、0.3662109375 mm 行间距条件下，理论量级为：

| 项目 | 计算结果 |
| --- | --- |
| 行频 | 约 15.17 kHz（真车设计参考，超出当前 11 kHz 仿真验收要求） |
| 行间隔 | 约 65.92 μs |
| 4096 行图块周期 | 约 0.270 s |
| 图块输出频率 | 约 3.70 块/s |
| Mono8 原始图像数据率 | 约 62.14 MB/s，不含协议与归档开销 |
| 16 位存储原始数据率 | 约 124.28 MB/s，不含额外开销 |

若要求运动模糊不超过半个沿程像素，20 km/h真车参考速度下曝光需不超过约32.96 μs；当前10 km/h仿真速度下为约65.92 μs。这是基于平稳直线运动的计算约束，不代表已选相机、镜头和补光一定满足。

### 5.5 镜头覆盖与安装几何

相机名义安装方向为垂直向下，感光线横跨扫描方向，地面有效扫描宽度为 1.5 m。4K 只说明像素数，不能单独确定传感器物理宽度、镜头焦距或安装高度。

- 设横向像元间距为 `p`，传感器有效长度为 `S = 4096 × p`。镜头有效像场必须覆盖整条传感器（考虑有效感光区域对角线和装配余量），且边缘分辨能力、暗角及工作距离满足要求；接口可安装不代表像场足够。
- 在垂直观察平面、忽略畸变的针孔近似下，地面幅宽 `W ≈ H × S / f`，因此 `H ≈ W × f / S`。`H` 是光学投影中心到地面的距离，不是车顶高度；`f` 为近似有效焦距。单位必须一致，近距离对焦时需使用标定内参及实际工作距离修正。
- 优先选型方向为 **20 mm 焦距且像场足够的镜头**。若像元间距继续暂取 7 μm，则有效感光长度 `S = 28.672 mm`，镜头像场须覆盖约 28.7 mm 的感光线并留装配余量；同时检查实际接口、对焦范围、边缘分辨能力和暗角，不能只按焦距选镜头。
- 按上述针孔近似，20 mm 焦距覆盖 1500 mm 时，光心离地约 **1046 mm**。这是优先安装几何方案，不是镜头前端离地距离或已确定的机械工作距离；实际高度仍需依据真实镜头和标定结果修正。
- 当前运行配置已迁移到 **20 mm / 光心高度约 1046 mm**，支架与安装外参由同一配置同步生成，标定版本更新为 `nominal-linescan-4k-20mm-large-v7`。旧版不同安装几何的测试保留为历史记录；历史结果见 [20 mm 与跑道分块说明](docs/archive/stage2/STAGE2_TILES.md)。旧前置 LED 的平面相对光带和传感器响应见 [补光原型](docs/archive/stage2/STAGE2_RADIOMETRY.md)，复杂地形覆盖仍待验证。
- 原型按像元间距、有效像素数、焦距和安装外参共同生成像素射线与视野，不能独立填写相互矛盾的高度和 1.5 m 幅宽。后续结合真实相机和镜头参数确定可用组合，并调整支架模型；第一阶段占位相机的位置不能作为最终光学安装依据。
- 用地面标尺验证完整有效扫描宽度和横向采样间距；加入横向畸变后核验边缘覆盖。坡度、俯仰、侧倾和高度变化时保留真实投影变化，不强行锁定所有姿态下均为 1.5 m。

## 6. GZ 线阵传感器方案

采用自定义线阵传感器或系统插件，使用 GZ 场景、材质、光照和运动状态生成采集数据。标准面阵相机不能直接代表完整的编码器触发线阵链路。

拟实现过程：

1. 从驱动轮编码器累计计数求出行触发事件与对应仿真时刻。
2. 获取或插值该时刻的相机真实姿态。
3. 按相机内参、光学畸变及地表生成一条有效扫描线。
4. 在插件内部累计连续扫描行，达到配置行数后发布图像和标签。
5. 独立发布模拟定位数据，供控制、归档与后处理使用。

已有窄条 Ogre2 渲染后端保留为材质与遮挡参考；高行频路线采用批量地表射线采样，当前 CUDA 实现仅支持无遮挡网格平面，后续必须补齐并验证材质、光照与遮挡能力。窄条渲染需要正确设置像素射线，不能把普通相机整个垂直视场压缩到一行。光学畸变采样需要的渲染边界与有效视场应在原型中验证。

以下约束必须满足：

- 不使用低帧率面阵图反复裁行来假装高行频采集。
- 一个物理步内可出现多个行触发，须按各自时刻处理，不全部使用同一姿态。
- 功能与几何验证允许批量生成和低于实时的仿真，优先保证仿真时序与采样正确；这一用途不替代 §6.1 的墙钟吞吐验收。
- 插值不能创造物理仿真未解析到的高频运动，振动验证需匹配物理更新频率。
- 生成图像可以使用仿真真值；控制、定位和校正拼接不得直接读取真值位姿或地面原始贴图绕过测量链路。
- 真值仅供传感器生成及独立评价使用；算法所需地表模型必须说明其合法来源。
- 轮胎打滑时仍按编码器触发，不用真实地面距离悄悄修正触发。

功能正确性、墙钟吞吐、运动时间保真度分别报告。11 kHz 只表示成像/处理能力，不表示物理系统解析了 11 kHz 运动；振动频谱、物理更新频率与允许几何误差须独立声明。离线重放不能仅依靠图块少量标签恢复任意高频运动，应保留足够的测量轨迹、触发规律和动态场景状态。

正式 CUDA 平面采集遇到任一曝光样本无法覆盖有效地表时拒绝整批并停止采集，不将部分曝光平均为暗像素。噪声以显式采集编号索引，预热不消耗编号。等待队列按行数、任务数和墙钟等待时间共同约束，批次与缓存容量配置化；故障后仅在明确重新启用时重建采样器，记录中断与编号缺口，禁止默默续接为完整扫描段。OptiX 正常背景、任意几何与有效性掩码的正式策略在接入阶段另行验证。

### 6.1 行频性能验收目标

当前验收基线为 **4096 像素/行、名义幅宽 1.5 m、行间距 0.3662109375 mm**，要求持续行频 **达到 11 kHz**。2026-09-08 调整：取消 22 kHz 验收要求，历史 22/23 kHz 测试仅保留为当时条件下的记录。旧模型20 m全宽原图链路已专项通过，新版20 mm/v7独立11 kHz仍待复测；CUDA静态网格子项不能替代材质、遮挡与补光验收。

| 验收档位 | 持续有效行频（墙钟时间） | 等间距采样对应车速 | Mono8 原始数据率 |
| --- | --- | --- | --- |
| 当前验收目标 | 11,000 行/s | 约 14.50 km/h | 45.056 MB/s |

当前整车仿真上限为 10 km/h，编码器等距离触发约 7.59 kHz。新版整车GUI＋RViz已验证10 km/h基础运动及0.5 m/s水泥路采集，10 km/h稳速20 m成像已通过，长时/多轨闭环仍待实现；11 kHz 墙钟能力通过独立传感器测试台验证。取消 22 kHz 要求，不提高当前整车限速，也不能将 7.59 kHz 供给下的实测冒充 11 kHz 验收。

验收须包含场景采样、已声明的光学与曝光/补光处理、GPU 读回、完整图块 ROS 接收与归档，不能只统计触发计数或 GPU 内核速度。记录硬件、场景复杂度、曝光采样数、数据格式、输出介质、测试时长和处理链路；明确归档是否仅写入系统缓存。预热后持续测量，排除缓存短时突发，队列不得持续增长，不得丢行、重复旧行或降低分辨率。只通过网格或简化光照的结果应标为对应子项，不能替代完整成像链路验收。

按墙钟时间计算端到端有效行频，同时报告仿真触发行频、车速和实时率，不能以放慢仿真达到的名义行频冒充实时吞吐。内部允许多时刻批量采样、GPU 图块累计和批量读回；归档仍采用图块末行标签及少量中间标签，不要求每行保存完整位姿。

若可持续吞吐不足以覆盖当前扫描车速，应在保持 4K、1.5 m 幅宽和既定行间距的前提下降低扫描车速并留余量；低于 11 kHz 的运行只用于功能调试，不算通过最低性能验收。低于实时的运行可用于几何验证，但不能替代上述性能验收。

## 7. 光学畸变、运动几何与平场校正

2026-09-08确认：正式流程为**采集并归档原图，随后离线进行暗场/平场及横向光学畸变校正，再做运动补偿和拼接**。不要求在线校正，校正不进入实时控制链路。`correction_profile`默认留空；已有在线节点只保留为可选诊断工具。§6.1的11 kHz要求适用于原始成像/传输/归档，离线校正仅报告正确性、耗时和资源占用，不要求达到实时行频。

最终成果需区分线性算法数据与供查看的TIFF：校正、运动补偿及拼接在线性域完成，查看版在导出端统一应用显示亮度转换，避免原始线性DN直接显示偏暗或各图块单独拉伸形成亮度接缝。当前仅预览和样片已转换，TIFF导出按用户要求后置，尚未实现；详细待办见[离线校正文档](docs/archive/stage2/STAGE2_CONCRETE_CORRECTION.md#待办最终拼接与tiff显示输出)。

三类误差分别建模、标定和评价，不混用术语：

| 类别 | 含义与处理方向 |
| --- | --- |
| 镜头光学畸变 | 感光像素与观察射线之间的非理想映射 |
| 运动与地表几何变形 | 行距误差、打滑、偏航、颠簸、支架抖动、离地高度和地形变化 |
| 辐射不均匀 | 暗电平、像素响应、补光不均、坏点和曝光变化 |

首版理想安装模型中，光学畸变主要按横向一维非线性映射实现，每行重复使用同一映射。纵向由扫描运动形成，不给整张线阵图直接套用面阵的二维桶形或枕形畸变。

“横向一维光学映射”是明确的初始模型假设，不是所有真实线阵光学系统的普遍结论。后续若安装偏心或实际标定需要，应扩展为每像素空间射线模型。

运动校正依据采样与稀疏轨迹，将各行投影到已知平面或可用地表模型，再重采样为统一地面网格。仅有末行位置不足以精确恢复任意抖动。

平场校正使用适合线阵的暗场与亮场标定，保留曝光、增益、补光及安装工况。固定平场不保证消除室外动态阴影、反光或过曝。首版建议 LED 纹理成像；若采用线激光，应明确是照明还是轮廓测量，不能默认二者成像链路相同。

### 7.1 高亮度主动补光模型

已确认设计工况：使用短曝光与高亮度主动补光，在有效扫描带内，补光对相机成像的贡献显著强于环境太阳光。此要求针对扫描带及相机响应波段，不表示整个场景都需要强补光，也不是短曝光自动消除太阳光。补光与环境光的比例需参数化，首版相对模型设定扫描线内 LED / 日照至少 20，名义中心设为 40；不是对实物照度的标定值。

范围进一步收敛：只需一种补光，默认实现条形 LED，不要求另做可切换的线激光版本。下列线激光说明仅保留为替代方案背景，不属于当前实现或验收任务。

白天成像目标是显著压制环境阴影，使扫描结果中不出现影响巡检的太阳光阴影。验收时对同一地面设置有、无日照遮挡，比较固定曝光下的图像差异；首版 90% 环境遮阳测试要求亮区减黑电平信号变化的 P99 不超过 4%。高亮补光并不自动消除补光自身被遮挡造成的阴影，需通过灯具位置和光带覆盖验证，不应关闭所有阴影或事后抹除阴影来伪造效果。

- **条形 LED**：相机垂直向下拍摄，灯具通过两侧短支架固定在车体前沿附近、位于相机扫描平面后方，比相机更接近地面，向前下方斜射，使地面长条光斑覆盖相机扫描线。光斑横向覆盖完整1.5 m名义视野（含畸变原始幅宽约1.56 m）并留余量，沿行进方向必须具有一定宽度（用户所述光斑“高度”），不能简化为无限细线；安装高度、前后偏移、俯角及横纵光束角均可配置。首版可用沿灯条排列的聚光灯近似，或用自定义着色器计算有限长度光带的照明分布；两者均需验证高度、入射角、遮挡和横向不均匀响应。
- **线激光照明**：用光源坐标中的扇形光束或薄光平面与地表相交，生成随高度和地形变化的窄光带。可研究投影纹理或自定义着色器实现，但投影贴图本身不等于正确的光照模型；需显式处理可见性、地表反射与衰减。首版不宣称模拟相干干涉和真实散斑，必要时另加经实测标定的统计模型。
- **传感器响应**：在线性强度域累积环境光与补光，经曝光积分、响应增益、噪声和饱和处理后量化为图像。不能只调亮最终 8 位图，或把已经饱和的渲染结果乘小来模拟短曝光。
- **曝光机制**：曝光时间须实际参与亮度与运动模糊计算；自定义传感器不能仅保存一个无效的曝光参数。连续补光与同步脉冲补光分别配置，脉冲模式需要定义与行触发的相位和脉宽。
- **验证**：用相同曝光分别采集环境光、补光及二者共同开启的图像，测量扫描带内的亮度贡献、均匀性、信噪比和饱和比例；在凹坑、板缝、坡面与支架抖动条件下检查光带和视线对齐。

光带沿程宽度与覆盖余量根据地面高度范围、车身俯仰/侧倾、支架相对抖动、曝光期间的位移及安装误差确定，具体数值待原型验证。平地名义安装时，灯具前置距离为 `d`、离地高度为 `h`，指向扫描线中心的光轴相对竖直向下的倾角近似满足 `tan(alpha) = d / h`。光束边界与地面相交决定实际光斑范围；斜射情况下不能把灯具光束角直接当成对称地面带宽。验收要求在声明的姿态和地形范围内，整条有效扫描线始终落在满足亮度与均匀性门限的光带内部，并检查车体、轮组和地面缺陷的遮挡。相机视线与补光光轴无需重合，但必须看向同一有效扫描带。

GZ 渲染数值不直接作为校准过的 lux 或 W/m²。首版以相对照明比例和传感器响应验证算法，实车参数到位后再拟合辐射响应。默认不依赖抬高全局环境亮度或把地面设置为自发光来代替随车补光。

## 8. 水泥路面场景

目标场景为真实感水泥板块路面，包含水泥颗粒纹理、裂缝、凹坑、板缝、污渍、磨损和修补痕迹。

2026-09-08裂缝修订已完成并应用20 m全宽道路；原始要求为保持细长、0.8–2.0 mm宽的要求，增加自然分叉、宽度变化及少量边缘破损/积灰；裂缝原始外观继续使用生图工具。由同一轮廓生成贴图和沟槽，避免固定截面的整齐刀口。先完成局部真实采集样片、尺度核对及资源成本复核，经用户确认外观后再整路烘焙，见[纹理计划](docs/archive/stage2/STAGE2_TEXTURE_PLAN.md)。

- 使用具有明确物理尺度的高分辨率真实地面贴图，记录来源与使用许可。
- 贴图采用0.25 mm/纹素，细于约0.3662 mm/像素的采集尺度，不能以低分辨率图放大代替真实细节。
- 凹坑、板块高差和需研究深度效应的板缝使用真实网格或高度场；普通颜色或法线贴图不代替碰撞几何。
- 视觉几何与碰撞几何应保持有意义的一致性，细微缺陷可根据研究目的简化碰撞。
- 已确认毫米级细裂缝保留GZ视觉/OptiX精细沟槽，轮胎碰撞可采用显式浅凹陷代理；当前试片偏差上限2 mm。碰撞资产、尺度及近似范围必须归档并自动检查，不得将该策略无条件用于较深坑洞、台阶或坡面。
- 尽量避免大范围机械重复纹理造成不符合实景的匹配特征。
- 另设尺寸与位置已知的诊断场景，包含标尺、已知裂缝和板缝，用于独立评价尺度、畸变、覆盖率与接缝误差。
- 默认渲染不视为真实太阳光、湿地反射或激光散斑的精确物理复现。

### 8.1 跑道规模与分块加载

当前已实现并测试 100 m × 10 m 网格纹理的实际分块读取、GPU 预取、有界缓存及局部坐标采样；GZ 使用有界异步采样队列。实现与边界见 [分块原型](docs/archive/stage2/STAGE2_TILES.md)。这不等于真实水泥、地形遮挡和完整大场景任务已验收。

最终验收采用跑道形长条巡检区域，代表性基准为 **100 m × 10 m**，并支持更宽区域；长度和宽度均可配置，不能把 20 m × 20 m 调试网格或 10 m 跑道宽度写死为系统上限。最终扩展宽度及场景组合按实际任务确定。

- 按当前0.25 mm/纹素估算，100 m × 10 m单层全覆盖约需160亿纹素；未压缩Mono8约16 GB，RGB8约48 GB，不含几何、其他材质通道、缓存和跨轨迹重叠。这是容量量级估算，不要求将全场景一次性放入显存。
- 地面纹理和必要的高程/网格按物理坐标分块组织，GPU 仅保留当前扫描区域及预取邻域；根据车速、扫描方向、曝光范围和预计加载延迟提前加载，缓存容量有界。远处可使用低细节显示，实际扫描区域必须保持声明的采样细节。
- 分块边缘使用一致坐标和足够的邻接数据，验证纹理插值、光照与几何在边界处连续。分块不能造成图像接缝、重复扫描行或改变网格尺度。
- 预取未完成或加载异常时必须显式记录并采取受控等待/减速/停采，不允许使用空白、旧块或低分辨率替代图像来维持表面行频。
- 大场景采用全局位置与局部计算坐标相结合的方式，GPU 射线计算在局部坐标完成，保留到全局三维坐标的变换，避免大坐标降低亚像素几何精度。
- 采集归档和最终拼接也采用分块读写与有界内存，不要求一次性将整条跑道图像装入内存。

### 8.2 大场景验收

**正常扫描跨块时，不得因分块加载出现车体运动卡顿、仿真时间停顿、采集或输出中断、图像闪变、坐标跳变和拼接接缝。** 此处指加载引入的异常，不要求消除被测车体本身真实的颠簸。预取不足后的受控等待/减速/停采仅作为异常处置，必须记录，该次连续性能测试判为未通过，不能作为常规加载流程。

- 读取、解码及 GPU 上传在后台预取，不把磁盘读写、解码或大块上传放入物理步与采集关键路径。根据扫描足迹、速度、方向、姿态范围和实测加载延迟尾部值确定预取距离并留余量；任务开始前确认起始区域已就绪。
- 使用当前可读块与待就绪块分离的缓存，确认新块完整上传后再切换映射；GPU 仍在使用的块不得提前覆盖或释放。切换时保持地面物理坐标、纹理采样、曝光时间和全局行号连续。
- 跨块不得重置触发相位、清空未完成图块或改变位姿参考；局部计算坐标切换需同步变换相关数据，不能让相机或地面产生跳变。
- 验收既检查持续平均行频，也记录物理步/仿真时钟推进的墙钟间隔、单批处理延迟、图块接收间隔及其最大值和高分位值，专门检查块边界附近的异常。允许的时延和抖动预算须在测试前结合块大小、行频和链路余量明确，不能仅凭平均值达到 11 kHz 判通过。

在 100 m × 10 m 代表场景及声明的更宽场景上验证完整覆盖、换行、避障、图像采集和拼接。性能验证须实际经过多次纹理/几何块边界，覆盖冷启动加载、稳态预取及沿程持续扫描，记录加载延迟、显存和主存峰值、队列长度、丢行及接缝误差。不能以重复读取同一小块网格替代大场景验收。

分块加载、GPU 采样、补光处理、图块输出与归档应共同满足第 6.1 节的 **11 kHz** 要求；11 kHz 能力通过独立采样测试验证，不提高当前整车仿真 10 km/h 的速度上限。场景扩大后必须复测，现有 23 kHz 网格子项结果不构成大场景性能承诺。

## 9. 覆盖规划、避障与拼接

- 区分可行驶区域、待巡检区域、相机计划扫描足迹和实际有效覆盖。
- 路径同时考虑底盘外形、转向过渡、相机安装偏置、扫描宽度与扫描方向。
- 默认采用有侧向重叠的平行扫描轨迹，换行与避障阶段停采。
- 暂停、恢复、绕障后记录实际缺口，并安排补扫或明确报告不可覆盖区域。
- 避障成功不等同于完成巡检；障碍传感器方案待选，不能把定位 GNSS 当作障碍感知。
- 同轨迹连续图块按行序与几何重建，不依赖不存在的前后重叠。
- 不同轨迹通过侧向重叠做局部匹配及全局优化；优化粒度应能表达线阵条带误差，不能默认每块都是刚性面阵照片。
- 输出保留物理尺度、覆盖掩膜、缺口和质量报告，不用填充图像掩盖未采集区域。

## 10. 已有项目的借鉴边界

相邻项目为 `../climbot_sim` 与 `../slam`。仅在核对依赖与接口后提取可复用内容，不整体照搬。

| 来源 | 可借鉴内容 | 必须适配的部分 |
| --- | --- | --- |
| 爬壁机器人 | 区域覆盖、扫描段管理、任务状态、RViz 交互 | 墙面坐标泛化为地表坐标，运动模型替换 |
| 爬壁采集归档 | 原图不可变、任务绑定、校验、归档流程 | 面阵触发替换为连续线阵采集，扩展行号与时间语义 |
| 爬壁图像处理与拼接 | 标定校验、匹配、优化、分块输出及评价组织 | 重写线阵投影与运动校正，重新设计无前后重叠的连续铺放 |
| 差速 SLAM 项目 | 障碍感知、导航组织、定位和回归场景经验 | 四轮里程计、横向运动、有限转角与坡面约束 |

不得将已有项目的阶段性验收结论视为本项目已通过验证。

## 11. 实施与验证顺序

当前近期顺序（2026-09-08按用户修正）：新版模型/GUI复核及新光学采集与标定复测（旧模型20×10 m基线已完成） → 矩形区域/相机足迹规划与CLI输入 → 轮里程计＋IMU＋RTK融合定位 → time轨迹跟踪、任务状态机、横移＋180°换行与采集门 → RViz任务交互及完整区域采集验收 → 离线校正/拼接/TIFF → 障碍处理与100×10 m验收。用户确认默认轨迹间距1.00 m，可配置；矩形长度沿道路方向，无需用户输入方向角。GNSS RTK 10 Hz、FIX水平2–3 cm及垂直3–5 cm，噪声/时序建模见[定位噪声基线](docs/LOCALIZATION_NOISE_BASELINE.md)。已完成的双轨编码器脚本不是正式任务执行器。详细设计见[矩形任务方案](docs/archive/integration/RECTANGLE_MISSION_PLAN.md)。

分叉裂缝已完成独立局部试片及成本复核：16点条光下增加约16.51 MiB显式GPU分配、平均批次耗时增加约1.65%，45.7 s独立长测约45.9 k行/s。外观已获用户认可，已应用20 m全宽道路；该局部统计不代表100 m全宽或新版模型11 kHz整链路验收，见[试片记录](docs/archive/stage2/STAGE2_BRANCH_CRACK.md)。

试片整车回退已在局部解决：精细分叉GUI约0.82、无GUI约0.84；按用户建议仅将轮胎碰撞面简化后均恢复约1.00，成像沟槽不变。声明并校验碰撞代理的误差界，保持较深凹坑/障碍物的有效碰撞，详见[碰撞简化记录](docs/issues/COLLISION_PROXY.md)。尚不代表整路或10 km/h验收。

1. **AGV 基础模型与控制**：生成参数化八关节底盘、相机和灯具安装结构，验证质量惯量、接触、直行、横移、斜行、旋转及限位解切换。产物是能受控运动、反馈关节状态的底盘。
2. **独立线阵相机与补光原型**：先确定像元尺寸、镜头像场、焦距与安装高度的参数化关系，验证垂直视线和车头近置、向前斜射 LED 光带覆盖；再在独立直线运动测试台上建立带纹理和标尺的平面，以 4K / 1.5 m、纵横等间距采样验证短曝光、主动补光主导、横向光学畸变、行触发、连续图块、末行标签及最高行频性能。原型不依赖完整导航和融合定位，可在第一阶段接口明确后交错推进。真值运动仅用于测试台成像和评价，不作为生产算法输入。
3. **矩形规划与融合定位**：输入区域角点、长度、宽度与固定间距（方向由道路地图配置提供），生成相机扫描足迹和带转向空间的底盘路径；接入轮里程计、IMU和RTK的局部/全局融合，先零噪声功能基线，再确定噪声。
4. **矩形任务执行与采集**：time参数化轨迹＋位姿闭环、分层状态机、横移＋车身180°换行、CLI/RViz交互、暂停恢复与采集完整性；先通过无障碍矩形任务，不将脚本演示称为完整规划。
5. **离线拼接与障碍扩展**：矩形采集通过后，离线进行暗场/平场、横向光学与运动校正、条带拼接和TIFF输出；继续引入障碍感知、绕障和缺口补扫。
6. **完整场景与性能验收**：在 100 m × 10 m 代表跑道及声明的更宽场景中，验证分块加载与跨块连续性、完整任务、大幅拼接、已知缺陷保真、覆盖率、存储吞吐和仿真实时率；从水平面逐步扩展到倾斜及高度变化路面，按轮地接触、通过能力和成像有效性逐项验收。

每阶段完成适当测试后再声明完成。长时间构建、测试或仿真完整输出保存到日志，成功时只报告简洁结果；失败时针对性分析日志。

正式验收前需量化：轨迹误差、限位余量、丢行率、标签时序误差、定位记录插值误差、地面尺度误差、跨轨迹接缝误差、有效覆盖率、仿真实时率和数据吞吐。门限根据目标与独立验证数据确定，目前不预设未经验证的精度承诺。

## 12. 待确认事项

- 最终车体尺寸、质心、轮径、减速比、编码器分辨率、转向零位与机械端点。
- 驱动与转向的速度、加速度、力矩、制动和悬挂参数。
- 相机型号、单线或多线结构、像元尺寸、位深、最高行频、曝光与触发时间戳定义。
- 按20 mm 焦距方向确定具体镜头，核对像场、边缘成像质量、实际工作距离、安装高度、支架刚度和 1.5 m视场在不同高度下的变化。
- 扫描运行速度、图块行数（暂定 4096）、默认条形 LED 的离地高度、前后偏移、入射角、光斑沿程宽度、覆盖余量及强度参数；定位轨迹频率、阴影抑制门限和允许的图像误差。
- RTK 航向来源、地表测距或建图方式、障碍传感器配置。
- 跨轨迹重叠率、地面素材、100 m × 10 m 基准之外的扩展跑道宽度、分块尺寸与缓存预算、数据存储容量及具体性能测试工况。

## 13. 技术参考

- [gz_ros2_control：关节控制与执行器扩展](https://control.ros.org/jazzy/doc/gz_ros2_control/doc/index.html)
- [Gazebo Sensors：自定义传感器](https://gazebosim.org/api/sensors/8/custom_sensors.html)
- [Gazebo Rendering：投影纹理接口（照明原型候选）](https://gazebosim.org/api/rendering/8/classgz_1_1rendering_1_1Projector.html)
- [Basler：线阵触发与编码器采集示例](https://docs.baslerweb.com/line-scan-gige-use-cases)
- [Common Vision Blox：线阵相机标定](https://help.commonvisionblox.com/NextGen/15.0/md_documentation__sections__camera_calibration__linescan_calibration.html)
- [MVTec：线阵成像与三维视觉指南](https://www.mvtec.com/fileadmin/Redaktion/mvtec.com/products/halcon/documentation/solution_guide/solution_guide_iii_c_3d_vision.pdf)

参考文档用于说明技术路线，不代表本项目实现或性能已经验证。

### 2026-09-06 标定阶段实现记录

- 以实际 CUDA 暗场、亮场和条纹板图像估计逐列响应及横向重采样表，保留仿真畸变参数与拟合流程的独立性。
- 本轮为固定高度平面横向度量标定，不声称已单独求得焦距、相机高度或完整外参；姿态变化补偿另行实现。
- 校正维持所有扫描行、块间连续性和末行参考标签；配置变化须检查标定适用性，不逐张自动调整亮度。
- 含校正和两份图像持久化的 CUDA/ROS 子测试已达到 22.99 kHz；GZ 控制并行负载、真实水泥纹理、运动补偿及多轨迹拼接未完成。详细证据见 [标定阶段记录](docs/archive/stage2/STAGE2_CALIBRATION.md)。

阶段性修复进展：采集有效性、噪声编号、等待队列与故障恢复见 [Review 第一批修复](docs/archive/stage2/STAGE2_REVIEW_FIXES.md)。

共享场景进展：静态地面和遮挡物已由同一网格供给 GZ 视觉/碰撞与 OptiX，并进行几何交叉校验，见 [共享静态场景](docs/archive/stage2/STAGE2_SHARED_SCENE.md)。AGV 动态状态与正式 OptiX 采集接入进展见 [OptiX 集成](docs/archive/stage2/STAGE2_OPTIX_INTEGRATION.md)。材质光度一致性和完整性能验收尚未完成；下一步回到水平网格路面验证高速连续采集与 ROS 接收完整性，不为起伏路面修改运动判定或放宽门限。

当前安装：光心名义高度1.046317 m，扫描平面x=1.15 m；支架固定在前端立面，不接可开启斜盖。条光长1.20 m、中心x=1.069615 m、名义离地0.30 m、向前偏离竖直15°，名义光带1.80×0.16 m。GPS在后平台x=−0.85 m、间距1.10 m、顶端名义1.20 m；电池舱静载离地约0.25 m。使用v7配置，旧平场不可复用。

旧16 mm模型校正进度（历史）：已使用真实 GZ / OptiX 图像完成 16 mm 固定平面横向标定、暗场和平场标定，并通过 10 km/h 无 GUI 的在线校正与完整图块接收测试。独立验证板最大误差约 0.50 px，独立亮场横向变异系数约 0.059%。详情见 [OptiX 标定记录](docs/archive/stage2/STAGE2_OPTIX_CALIBRATION.md)。这不是完整内外参分离、运动补偿或 11 kHz 全链路验收。

材质试验进展（2026-09-08）：Concrete047A 颜色、法线和粗糙度已接入小范围 OptiX 世界 XY 投影采样，使用同一拼贴布局及真实裂缝/板缝沟槽，详见 [材质成本记录](docs/archive/stage2/STAGE2_PBR_COST.md)。这是实验性局部材质，不是全跑道UV流式缓存、GZ材质一致性或11 kHz完整链路验收。

当前材质选择已确认：后续共享场景默认使用 Concrete047A 颜色＋法线，统一粗糙度 **0.60**，不默认加载粗糙度地图。历史材质对比保留其试片均值 0.599787 和原始统计，不重写实验参数。小范围 GZ/OptiX 同源材质及低速连续采集已完成；100 m中央走廊有界流式缓存已通过独立11 kHz原图链路验证，范围和整车测试见 [流式道路记录](docs/archive/stage2/STAGE2_STREAMED_ROAD.md)。

道路布局已确认：100×10 m 目标采用双向两车道，中央黄色双实线（各宽0.15 m、净距0.15 m），两侧白实线（宽0.15 m、中心距路边0.5 m）；5×5 m板块中央纵缝置于双黄线间隙，允许横缝与标线相交。10×10 m同源材质及低速GUI整车采集已通过，范围与证据见 [共享道路记录](docs/archive/stage2/STAGE2_SHARED_ROAD.md)。

100 m共享道路的中央1.536 m高精度走廊已采用32槽颜色＋法线流式缓存：独立原图落盘/ROS链路持续11.2 kHz；10 km/h、60 m整车无GUI及GUI＋RViz连续采集通过。尚不代表全10 m宽、校正/拼接全链路验收，详见 [流式道路记录](docs/archive/stage2/STAGE2_STREAMED_ROAD.md)。

旧16 mm水泥材质标定复核及整段离线校正已完成；新20 mm/v7标定未完成，默认4096×4096、可配置行数、短尾块和稀疏标签保持，记录及使用入口见 [水泥路面离线校正](docs/archive/stage2/STAGE2_CONCRETE_CORRECTION.md)。

20×10 m短路段已扩展全宽颜色＋法线、新版分叉沟槽和显式碰撞代理，独立原图链路通过11 kHz；GUI＋RViz相邻正反向轨迹已获得连续原始档案。100 m全宽、相邻轨迹离线配准/拼接和最终TIFF仍待完成，详见[全宽道路记录](docs/archive/stage2/STAGE2_FULLWIDTH_ROAD.md)。

2026-09-08架构补充：三维地表规划与接触约束控制从首版设计，平面仅为当前场景；真机双天线RTK承担绝对航向初始化与辅助。线阵拼接优化连续扫描轨迹并逐行重投影，不能按4096行文件各自自由移动；采用侧向匹配、导航/编码器先验及地表约束，记录不可观测方向和真实缺口。研究来源和详细边界见[三维、航向与连续条带设计](docs/SURFACE_HEADING_STRIP_DESIGN.md)。

双天线安装补充：当前550 kg模型尾部x=−0.85 m平台安装，用户最终基线1.10 m、外壳顶端名义离地1.20 m；需补偿横向基线到车头方向的安装关系和主副顺序。航向模型、杆臂和视觉安装须一致，航向不确定度由相对基线与安装外参传播。电池舱正常负重静止离地约0.25 m。相机20 mm/1.5 m、条光1.20 m灯体及1.80 m名义光带详见重建记录。

第二步定位与闭环验收必须包含安装/标定外参残差，覆盖双GNSS共同支架与相位中心、相机支架、IMU、轮组及双雷达。零残差只作理想对照；实际生成几何与算法标定值分离，固定安装偏差不按每帧白噪声处理。具体方案与接入边界见[外参误差设计](docs/LOCALIZATION_NOISE_BASELINE.md#安装外参与标定残差第二步必做)。

用户确认最大加速度0.8 m/s²、最大减速度1.0 m/s²，控制器独立配置两者；10 km/h理论制动距离约3.86 m。当前优先轨迹规划、三维融合定位/固定外参误差、闭环控制与原图采集，往返照明匹配留至拼接阶段，支架振动不做。详细交付、顺序与验收见[实施计划](docs/MISSION_IMPLEMENTATION_PLAN.md)。


单段时间闭环已接入融合定位：梯形/三角形速度曲线给出各时刻参考位置，叠加位置/航向反馈；加速度0.8、减速度1.0 m/s²，底层对轮期间暂停并重新定时。终点须位姿与停稳共同确认，微调也使用独立时间曲线。当前验收范围为0.5 m/s平移、旋转及故障停车，完整矩形执行、扫描联动和提速仍按计划推进，见[闭环记录](docs/archive/integration/TRACKING_IMPLEMENTATION.md)。

矩形执行与采集联动进展：连续PASS连接加速、扫描、驶出，换道停车横移后180°转身，原图尾块确认关闭；新增四轮编码器纵向投影及独立融合标签。ACQUIRED不自动代表覆盖审计通过，真实任务还需覆盖检查与异常补扫，详见[矩形采集](docs/RECTANGLE_CAPTURE.md)。

扫描稳定性修订：继续使用全局融合位置；降低全局EKF水平/航向过程噪声并缓和反馈，不降低RTK测量噪声冒充精度。换道前延长前向驶出，不再掉头后倒退；预览默认8倍面积缩小至512×512。条光为0.30 m/偏竖直15°，支架与光源模型共同更新，详见[稳定性记录](docs/issues/SCAN_STABILITY.md)。

转向余量现行约定：以±190°硬限位为基准保留8°静止目标余量；±185°软限位不变，从软限位计算的固定余量为3°，允许±180°等价分支配合驱动反转。连续运动仍另加转向制动距离及一个控制周期，见[验证](docs/issues/STEERING_SPIN_RECOVERY.md)。
